import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'l02-assignment',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
